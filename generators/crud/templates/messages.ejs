<%_ let lastField = fields[fields.length - 1] -%>
  <%= serviceName %>_title: '<%= serviceNameTitleCase %>',
<%_ listFields.forEach(e => { -%>
  <%= serviceName %>_<%= e.field %>: '<%= e.fieldTitle %>'<%_ if (e.field !== lastField.field) { -%>,<%_ } -%>
<% }) %>
}
